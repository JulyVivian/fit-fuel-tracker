import { createStore, Store } from 'vuex';

interface State {
  // 定义状态的类型
}

export default createStore<State>({
  state: {
    // 初始状态
  },
  mutations: {
    // mutations
  },
  actions: {
    // actions
  },
  modules: {
    // modules
  }
});